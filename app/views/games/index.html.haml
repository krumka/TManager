.row.title-page
  %h1.col-sm-9
    = @tournament.nil? ? "Listing games" : "Listing games for " + @tournament.name

  .col-sm-3= link_to 'New Game', new_game_path, class: "btn-up btn btn-primary"

-if @games.empty?
  .row
    .col-sm-12.text-center
      %h2 No games found
-else
  - @games.each do |game|
    .row.game
      .col-sm-4.title= link_to (image_tag game.image.url(:thumb)), game
      .col-sm-8.desc= game.description.first(195) + " . . . . "